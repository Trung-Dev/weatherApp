import { {{pascalCase prefix}}{{pascalCase name}} } from "./{{pascalCase prefix}}{{pascalCase name}}";

export default {
  title: "{{pascalCase type}}/{{pascalCase prefix}}{{pascalCase name}}",
  component: {{pascalCase prefix}}{{pascalCase name}},
};

const Template = (args) => <{{pascalCase prefix}}{{pascalCase name}} {...args}/>;
export const Default = Template.bind({});